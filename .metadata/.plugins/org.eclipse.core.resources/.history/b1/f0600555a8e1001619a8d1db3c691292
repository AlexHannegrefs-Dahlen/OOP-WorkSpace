package tic.tac.toe.view;

import tic.tac.toe.model.board.Board;

public class Viewer {
	public static void printBoard() {
		for (int i = 0; i < Board.getBoardspaces().length; i++) {
			System.out.print(Board.getBoardspaces()[i].getSymbol());
			if(i == 2 || i == 5)
				System.out.println("");
		}
	}
}
